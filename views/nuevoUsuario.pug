extends layout 

include mixins/crearUsuario

block contenido
    .contenido-principal
        h1 #{nombrePagina}

        if errores
            each error in errores
                .alerta= error.texto

        +crearUsuario(usuario)